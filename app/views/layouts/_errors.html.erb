<% if @article.errors.any? %>
    <h2>The following errors prevented the article to being saved.</h2>
    <ul>
        <% @article.errors.full_messages.each do |msg| %>
            <li> <%= msg %></li>
            <% end %>
    </ul>
<% end %>